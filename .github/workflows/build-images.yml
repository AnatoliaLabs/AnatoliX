name: Build all images

# todo: exclusive to current branch, but for now we're hackinng
on:
  push:

jobs:
  build:
    strategy:
      matrix:
        arch:
          - x86_64
          - aarch64
        variants:
          - name: gnome
            format:
              - diskimg
              - live-iso

    uses: ./.github/workflows/katsu.yml
    with:
      arch: ${{ matrix.arch }}
      variant: ${{ matrix.variants.name }}
      mode: ${{ matrix.variants.format }}
