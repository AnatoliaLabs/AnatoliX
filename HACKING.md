# building Ultramarine images

To build Ultramarine images, you will require either `katsu` or Docker/Podman in rootful mode.

You may also set up a _Podman Machine_ to run the builds entirely inside a virtual machine.


