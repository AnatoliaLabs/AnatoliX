builder: dnf

import:
  - ../base/base.yaml
  - ../live/live.yaml
  - ../base/desktop.yaml
  - ../live/readymade.yaml
  - gnome.yaml

distro: Ultramarine Linux Cheetah (GNOME Edition)

out_file: ultramarine-gnome-rawhide-live.iso

bootloader: grub

kernel_cmdline: "quiet rhgb"

iso:
  volume_id: UM-Gn-Live

scripts:
  post:
    - id: gnome-live
      name: Set up GNOME Live
      file: gnome-live.sh

dnf:
  releasever: rawhide
  exec: dnf5
