builder: dnf

import:
  - mt8183.yaml
  - ../../../xfce/xfce.yaml
  - ../../../xfce/base-disk-aarch64.yaml

distro: Ultramarine Linux Cheetah Xfce (mt8183 Chromebook)

out_file: ultramarine-cbe-mt8183-xfce-rawhide.img

bootloader: grub

kernel_cmdline: "quiet rhgb"

dnf:
  releasever: rawhide
  exec: dnf5
