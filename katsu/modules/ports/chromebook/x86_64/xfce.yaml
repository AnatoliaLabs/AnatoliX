builder: dnf

import:
  - ../chromebook.yaml
  - ../../../xfce/xfce-live.yaml
  - ../../../xfce/base-disk-x86_64.yaml

distro: Ultramarine Linux Cheetah Xfce (Chromebook Edition)

out_file: ultramarine-cbe-xfce-rawhide.img

bootloader: grub

kernel_cmdline: "quiet rhgb"

dnf:
  releasever: rawhide
  exec: dnf5
