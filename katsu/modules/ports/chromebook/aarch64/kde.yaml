builder: dnf

import:
  - aarch64.yaml
  - ../../../plasma/plasma.yaml
  - ../../../plasma/plasma-disk.yaml
  - ../../../base/base.yaml
  - ../../../base/base-disk-nokernel.yaml

distro: Ultramarine Linux 41 KDE (aarch64 Chromebook)

out_file: ultramarine-cbe-aarch64-kde-41.img

bootloader: grub

kernel_cmdline: "quiet rhgb"

dnf:
  releasever: 41
