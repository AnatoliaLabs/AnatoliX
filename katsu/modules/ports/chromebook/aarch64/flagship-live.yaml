builder: dnf

import:
  - ../chromebook.yaml
  - ../chromebook-rdm.yaml
  - chromebook-disk-aarch64.yaml
  - ../../../live/readymade.yaml
  - ../../../live/live.yaml
  - ../../../flagship/flagship.yaml
  - ../../../base/desktop.yaml
  - ../../../base/base.yaml

distro: Ultramarine Linux 42 Flagship (Chromebook Edition)

out_file: ultramarine-cbe-flagship-42-aarch64.iso

bootloader: grub

kernel_cmdline: "quiet rhgb"

iso:
  volume_id: UM-Flagship-Live

scripts:
  post:
    - id: flagship-live
      name: Set up Flagship Live
      file: ../../../flagship/flagship-live.sh

dnf:
  releasever: 42
  exec: dnf4
