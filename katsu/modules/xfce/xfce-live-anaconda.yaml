builder: dnf

import:
  - ../base/base.yaml
  - ../base/desktop.yaml
  - ../live/live.yaml
  - ../live/anaconda.yaml
  - xfce.yaml

distro: Ultramarine Linux 43 (Xfce Edition)

out_file: ultramarine-xfce-43-live-anaconda.iso

bootloader: grub

kernel_cmdline: "quiet rhgb"

iso:
  volume_id: UM-Xfce-Live-Anaconda

scripts:
  post:
    - id: xfce-live
      name: Set up XFCE Live
      file: ./xfce-live.sh

dnf:
  releasever: 43
  exec: dnf5
